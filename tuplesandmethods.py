# in tuples instead of square brackets we give round brackets

# tuples cannot be changed but are mutable like list

t = (1, 2, 3, 4)
print(t)
print(t.count(4))
print(t.index(4))


# tuples items cannot be changed and are immmutable.
# t[0] ='j' # will give an error
