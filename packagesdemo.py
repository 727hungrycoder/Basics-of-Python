# modules and pip
# pip is used to install the modules in python.Modules are inbuilt codes which are written by someone else
# use terminal to install the packages. Terminal is in the left corner.It is same as the powershell of the windows.
# type  command pip install pandas in terminal to download panda package
# Pandas is a Python library used for working with data sets. It has functions for analyzing, cleaning, exploring, and manipulating data. The name "Pandas" has a reference to both "Panel Data", and "Python Data Analysis" and was created by Wes McKinney in 2008.
# Comma-separated values is a text file format that uses commas to separate values. A CSV file stores tabular data in plain text, where each line of the file typically represents one data record. Each record consists of the same number of fields, and these are separated by commas in the CSV file
# for xl files install openpyxl module

# import pandas as pd
# pd.read_excel('data.xlsx')

